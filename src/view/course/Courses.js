import React from 'react'
import { Outlet } from 'react-router'

const Courses = () => {
    return (
        <div>
            This is Courses
            <Outlet />
        </div>
    )
}

export default Courses
